// code 是一个如下的字符串，只包含符号和数字
// 符号一共有 6 种 [ ] + - * /
// 数字就只有十进制数字
let code = '[+ 123 456]'
// 对于上面一行字符串，返回下面的内容，注意数字类型被转换了
// ['[', '+', 123, 456, ']']

const tokens4 = function(code) {
    let s = code
    let i = 0
}
